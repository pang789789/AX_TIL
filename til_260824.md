# 파이썬 기초 챕터 요약 노트: 자료구조 (리스트, 튜플, 집합)

## 1. 리스트 (List)
* **특징:** 대괄호(`[]`)를 사용하며, 순서(Index)가 있고 요소를 자유롭게 추가, 수정, 삭제할 수 있는 핵심 자료구조입니다.
* **for문과 인덱스 접근:**
  * 요소 직접 순회: `for item in items:` (값만 바로 활용할 때)
  * 인덱스로 순회: `for i in range(len(items)):` 또는 데이터 개수를 직접 아는 경우 `for i in range(4):` 와 같이 인덱서(`items[i]`)를 활용해 순서 제어 가능.

## 2. 튜플 (Tuple)
* **특징:** 소괄호(`()`)를 사용하며, 한 번 생성하면 요소를 **추가·수정·삭제할 수 없습니다(Immutable).**
* **사용 이유:** 데이터의 안전한 보존, 리스트 대비 빠른 속도 및 적은 메모리 사용.
* **요소가 1개인 튜플:** 단순 숫자 괄호 연산과 구분하기 위해 뒤에 **쉼표(`,`)**를 반드시 붙여야 합니다. (예: `(5,)`)
* **다양한 자료형 혼용:** `("문길동", 20, "Python")`과 같이 서로 다른 데이터 타입을 한 번에 담을 수 있습니다.
* **패킹 & 언패킹 (Packing & Unpacking):**
  * **패킹(Packing):** 쉼표로 나열된 여러 값을 하나로 묶는 것 (`data = 1, 2, 3`)
  * **언패킹(Unpacking):** 묶여 있는 값을 개별 변수로 푸는 것 (`a, b, c = data`)
  * **값 교환(Swap):** `a, b = b, a` 구문을 통해 임시 변수 없이 두 변수의 값을 손쉽게 교체 가능.

## 3. 집합 (Set)
* **특징:** 중괄호(`{}`) 또는 `set()`을 사용하며, **중복을 허용하지 않고 순서가 없습니다.**

### 집합 전용 주요 메소드 (Method)
> **메소드(Method)란?**  
> `자료형.메소드()` 형태로 마침표(`.`)를 찍어 사용하는 **해당 자료형 전용 기능**입니다.

* **`add(x)`:** 집합에 요소 `x` 1개를 추가합니다. (중복 값 추가 시 무시됨)
* **`remove(x)` vs `discard(x)` (삭제 메소드 차이):**
  * **`remove(x)` (엄격한 삭제):** 지우려는 값이 집합에 없으면 🚨 **`KeyError`** 발생.
  * **`discard(x)` (안전한 삭제):** 지우려는 값이 없어도 에러 없이 **무시하고 진행.**

---

## 💡 주요 자료구조 한눈에 비교

| 구분 | 리스트 (List) | 튜플 (Tuple) | 집합 (Set) |
| :--- | :--- | :--- | :--- |
| **기호** | `[ ]` | `( )` | `{ }` |
| **순서(인덱스)** | O (가능) | O (가능) | X (불가) |
| **중복 허용** | O (허용) | O (허용) | **X (자동 제거)** |
| **수정 가능 여부** | **O (가능)** | **X (불가)** | **O (가능)** |