# 📘 [학습 일지] AI와 함께하는 타이타닉 데이터 분석 (STEP 09 ~ STEP 17 완결)

> **일시:** 2026년 9월 14일  
> **주제:** EDA/Feature Engineering부터 머신러닝 모델링, 평가, 저장 및 Streamlit 웹 서비스 배포까지 (Part 2 & Part 3 완결)  
> **진도:** STEP 09 (기초 통계와 EDA) ~ STEP 17 (Streamlit 예측 앱 구현)  
> **핵심 원칙:** AI의 코드 제안을 바탕으로 검증·평가·선택의 주체로서 분석 파이프라인 전체 완주하기  

---

## 1. 수업 개요 및 학습 흐름

* **Part 2 (STEP 09~15):** 탐색적 데이터 분석(EDA)을 통해 가설을 세우고, Feature Engineering ➔ Train/Test Split ➔ Baseline 모델 ➔ 성능 평가 및 오분류 분석 ➔ 최종 모델 선정을 진행.
* **Part 3 (STEP 16~17):** 완성된 전처리 파이프라인과 모델을 저장하고, 이를 활용해 신규 입력 데이터 예측 및 Streamlit 대시보드 앱으로 서비스화.

---

## 2. STEP별 학습 노트 및 실습 기록

### STEP 09. 기초 통계와 EDA (탐색적 데이터 분석)
* **핵심 내용:** 단변량/다변량 집계 통계량을 확인하고, 생존율 차이에 영향을 주는 핵심 요인(성별, Pclass, Fare 등)의 패턴을 정리.
* **핵심 관찰:** 여성의 생존율이 남성에 비해 압도적으로 높으며, 객실 등급(1등석 > 2등석 > 3등석)에 따른 생존율 차이가 뚜렷함을 그룹별 통계치로 확인.

### STEP 10. Feature 설계 (Feature Engineering)
* **핵심 내용:** 기존 컬럼(Name, SibSp, Parch, Age, Cabin 등)을 조합하여 모델 학습에 유용한 파생 Feature를 생성.
* **주요 파생 변수:**
  - `Title`: Name에서 호칭(Mr, Miss, Mrs, Master 등)을 추출하여 연령대 및 사회적 지위 반영
  - `FamilySize`: SibSp + Parch + 1 (동승 가족 수)
  - `IsAlone`: 가족 없이 혼자 탑승했는지 여부 (1/0)
  - `Deck`: Cabin의 첫 글자를 추출하여 선실 구역 파악

### STEP 11. Train / Test 데이터 분리
* **핵심 내용:** Data Leakage(데이터 누수)를 방지하기 위해 학습용(Train)과 평가용(Test) 데이터를 엄격히 분리.
* **주의점:** Target(`Survived`)의 비율이 유지되도록 Stratified Sampling(`stratify=y`)을 적용. 전처리 기준(평균, 중앙값, Scaler 등)은 오직 Train 데이터 기준으로만 계산하고 Test 데이터에 적용(transform)함.

### STEP 12. Baseline 모델 구축 (Logistic Regression Pipeline)
* **핵심 내용:** 복잡한 모델을 쓰기 전, 최소한의 전처리와 수치 변환만을 포함한 단순한 Baseline 모델을 구축하여 기준 성능 설정.
* **구현 방식:** `scikit-learn`의 `Pipeline`과 `ColumnTransformer`를 활용하여 전처리부터 모델 학습까지 하나의 객체로 묶음.

### STEP 13. 성능 평가와 오류 분석 (Confusion Matrix & Error Analysis)
* **핵심 내용:** 단순 Accuracy(정확도)에만 의존하지 않고 Precision, Recall, F1-Score, ROC-AUC 및 혼동 행렬(Confusion Matrix)을 종합 분석.
* **오분류 분석:** 모델이 틀리게 예측한 승객 데이터를 역추적하여, 어떤 특징(예: 3등석 여성, 1등석 남성 등)에서 예측 오류가 발생하는지 확인하고 개선 가설 수립.

### STEP 14. 추가 모델 선택 및 실험
* **핵심 내용:** Baseline(Logistic Regression) 외에 Tree 기반 모델(Random Forest, Decision Tree, Gradient Boosting 등)을 추가 학습하여 성능 비교.
* **실험 기록:** 동일한 Train/Test 분리 조건에서 각 모델의 평가 지표를 기록하고 복잡도 대비 성능 이득을 판단.

### STEP 15. 모델 비교 및 최종 모델 선정
* **핵심 내용:** 단일 성능 지표뿐만 아니라 과적합(Overfitting) 여부, 모델 해석 가능성, 데이터 재현성 등을 종합 검토하여 최종 모델 선정.
* **최종 파이프라인 구성:** 선택된 최종 전처리 프로세스 + 머신러닝 알고리즘 조합 확정.

### STEP 16. 모델 저장과 신규 입력 예측
* **핵심 내용:** 선정된 최종 Pipeline을 `joblib` 등의 라이브러리로 파일(`titanic_final_pipeline.joblib`) 및 계약 명세(`titanic_model_contract.json`) 형태로 저장.
* **검증:** 저장된 모델을 다시 로드하여 새로운 승객 데이터(가상의 신규 사용자 입력)를 넣었을 때 정상적으로 생존 확률(`predict_proba`)을 출력하는지 재현성 검증.

### STEP 17. Streamlit 예측 앱 구현
* **핵심 내용:** STEP 16에서 저장한 모델 파일과 명세를 불러와 사용자가 웹 UI에서 승객 정보를 입력하면 실시간으로 생존 확률을 예측해 보여주는 웹 앱 제작.
* **핵심 포인트:** Notebook에서 학습할 때 쓰인 전처리 규칙과 Streamlit 서비스 앱에서 실행되는 전처리 규칙이 완전히 동일하게 작동하도록 Pipeline을 재활용.

---

## 3. 전체 실습 완료 소회 및 요약

* **파이프라인 완성:** 데이터 로딩부터 전처리, EDA, Feature Engineering, 모델 학습/평가, 모델 저장, 웹 UI 서비스 구축까지 **데이터 분석의 한 사이클(End-to-End Cycle)**을 완주함.
* **분석가의 역할 확립:** AI가 제공하는 코드는 도구일 뿐, 파이프라인 전체 흐름을 이해하고 각 STEP에서의 기준 설정과 모델 선택의 의사결정은 분석가 본인이 직접 수행해야 함을 경험함.