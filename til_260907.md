# 📘 SQL & pandas 데이터 분석 학습 정리 노트

---

## 1. PostgreSQL & SQL 기초 (Chapter 03)

### 1.1 SQL 기본 CRUD 및 데이터 제어
* **테이블 생성 및 입력:** `CREATE TABLE`을 통해 구조를 정의하고, `INSERT INTO`로 단일 또는 다중 행 데이터를 추가함.
* **조회 및 조건 필터링 (`SELECT` & `WHERE`):**
  * `WHERE`: 조건에 맞는 데이터만 추출.
  * 연산자: `AND`, `OR`, `IN`, `LIKE` (문자열 패턴 검색).
  * `NULL` 비교: `IS NULL` 또는 `IS NOT NULL`을 사용해야 함 (`= NULL` 사용 불가).
* **정렬, 중복 제거, 개수 제한:**
  * `DISTINCT`: 중복 행 제거.
  * `ORDER BY`: 특정 컬럼 기준 오름차순/내림차순 정렬.
  * `LIMIT`: 결과 개수 제한.

### 1.2 안전한 데이터 수정 및 삭제
* **`UPDATE` / `DELETE` 사전 검증:** 
  * 데이터를 수정하거나 삭제하기 전에 **동일한 조건의 `SELECT` 문을 먼저 실행**하여 대상 행을 반드시 확인한 후 진행함.

---

## 2. pandas 데이터 분석 (Chapter 04)

### 2.1 분석 구조 파악 및 주요 개념 구분
* **`orders` vs `order_items`:**
  * **`orders` (주문):** 결제 건당 1행 (영수증 1장). `order_id`, `customer_id`, `order_status` 보관.
  * **`order_items` (주문 상세):** 결제에 포함된 개별 상품 1줄당 1행. `order_id`, `product_id`, `quantity`, `unit_price` 보관.
* **지표 및 단위 정의:**
  * **매출:** 전체 금액이 아닌, `completed` 상태 주문의 `line_total` 합계.
  * **주문 건수:** 전체 행 수가 아닌 고유한 `order_id` 개수 (`nunique()`).

---

### 2.2 컬럼 선택 및 데이터 구조
* **Series vs DataFrame:**
  * **Series (1차원):** `df["city"]` (문자열 하나로 선택).
  * **DataFrame (2차원):** `df[["city", "age"]]` (리스트로 선택).
* **경로 결합 시 주의사항:**
  * `pathlib.Path` 객체와 문자열 결합 시 `+` 연산자는 `TypeError` 발생. 슬래시(`/`) 연산자(`path / "file.csv"`) 사용 권장.

---

### 2.3 필터링, 정렬, 파생 컬럼
* **조건 필터링:**
  * 비트 연산자 `&` (AND), `|` (OR), `~` (NOT) 사용 시 각 조건을 반드시 **괄호 `()`**로 감싸야 함.
  * 여러 값 포함 확인: `isin([...])` 사용.
* **파생 컬럼 생성:**
  * `df_work = df.copy()`로 안전하게 복사본을 만든 뒤 `df_work["line_total"] = df_work["quantity"] * df_work["unit_price"]` 계산.

---

### 2.4 그룹 집계 및 데이터 병합 (`merge`)
* **집계 메서드 구분:**
  * `count()`: 결측치 제외 값 개수.
  * `size()`: 그룹의 전체 행 수.
  * `nunique()`: 고유한 값의 개수.
* **`groupby()`:** 기준 컬럼이 결과 표의 '한 행의 의미'를 결정. `as_index=False` 옵션으로 일반 컬럼 형태로 결과 유지.
* **안전한 병합 (`merge`):**
  * `validate="many_to_one"`: 예상한 관계 수(다대일 등)와 맞지 않는 중복 키 발생 시 에러를 내어 검증.
  * `indicator="order_match"`: 연결 상태(`both`, `left_only` 등)를 추적하여 미매칭 행 감지.