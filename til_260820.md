# 파이썬 기초 핵심 개념 요약 노트

## 1. 변수 명명 규칙 및 예약어
* **공백(띄어쓰기) 금지:** 공백은 파이썬에서 문법을 구분하는 구분자(Separator)로 쓰입니다. 변수 이름에 공백이 들어가면 파이썬이 서로 다른 단어로 오해하여 `SyntaxError`를 일으킵니다. (예: `user_age` 형태로 작성)
* **예약어(Keywords):** `if`, `for`, `def` 등 파이썬 자체 구조를 이루는 명령어는 변수로 사용할 수 없습니다. (`SyntaxError` 발생)
* **내장 함수(Built-in Functions) 주의:** `print`, `type` 등은 변수명으로 지정할 수 있지만, 지정할 경우 기존 함수의 기능이 덮어씌워져 기능을 잃게 됩니다.
* **언더스코어(`_`) 활용:** 예약어나 내장 함수 단어를 변수로 꼭 쓰고 싶다면, `print_name` 또는 `class_`처럼 단어 뒤나 중간에 언더스코어를 결합하여 안전하게 우회할 수 있습니다.

## 2. 연산자 (Operators)
* **산술 연산자:**
  * `/` (나누기): 나누어떨어지는 계산이더라도 무조건 실수(`float`) 타입 결과로 변환됩니다.
  * `//` (몫 나누기): 소수점 아래를 버리고 정수(`int`) 몫만 구합니다.
  * `%` (나머지): 나눈 뒤 남은 나머지를 구합니다. (시간/분 단위 변환, 짝수/홀수 구분 등에 활용)
  * `**` (거듭제곱): 제곱 계산을 수행합니다.
* **비교 연산자:**
  * `!=`: "같지 않다"를 뜻하며 결과는 불리언(`True`/`False`)입니다. (※ 과거 파이썬 2의 `<>` 연산자는 파이썬 3에서 완전 삭제되었습니다.)
* **논리 연산자:**
  * `not`: 참과 거짓을 반대로 뒤집습니다. (`not True` -> `False`, `not False` -> `True`)

## 3. 문자열 및 출력 조작
* **`print()` 출력 공백 제어:**
  * `print()`에서 콤마(`,`)로 여러 항목을 출력하면 항목 사이에 자동 공백이 들어갑니다.
  * **f-string (가장 권장):** `f"{time_h}시간 {time_m}분"` 방식으로 작성하면 여백을 직관적이고 깔끔하게 조정할 수 있습니다.
  * **`sep=""` 옵션:** `print(time_h, "시간 ", time_m, "분", sep="")`을 사용해 콤마 사이의 자동 공백을 없앨 수 있습니다.
* **특정 문자의 뒤쪽 문자열 추출 (이메일 도메인 등):**
  * **`split("@")[1]`**: 구분 문자(`@`)로 분할한 리스트의 두 번째 요소(`[1]`)를 가져오는 방식이 가장 직관적이고 간단합니다.
* **`sub` 변수명의 의미:** 언어 차원의 고정 기능이 아닌 작성자의 의도에 따른 약어입니다. 보통 Subject(주제/과목), Substring(부분 문자열), Subtract(뺄셈/차이)의 뜻으로 쓰입니다.