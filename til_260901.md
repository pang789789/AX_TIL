# 📘 파이썬 학습 노트: 데이터 분석과 pandas 시작하기 (Chapter 21)

---

## 🎯 Chapter 21 학습 목표
* 데이터 분석에서 pandas가 어떤 역할을 하는지 설명할 수 있습니다.
* 프로젝트 가상환경에 pandas를 설치하고 `import pandas as pd`로 불러올 수 있습니다.
* Series와 DataFrame의 차이를 기초적으로 설명할 수 있습니다.
* `pd.read_csv()`로 CSV 파일을 DataFrame으로 읽을 수 있습니다.
* `head()`, `tail()`, `shape`, `columns`, `dtypes`, `info()`로 데이터 구조를 점검할 수 있습니다.
* 한 열과 여러 열을 선택하고 결과 자료구조의 차이를 설명할 수 있습니다.
* 비교식으로 Boolean Series를 만들고 원하는 행만 필터링할 수 있습니다.
* 여러 조건에서 `&`와 괄호를 사용할 수 있습니다.
* `mean()`, `min()`, `max()`, `sum()`, `describe()`로 숫자 데이터를 요약할 수 있습니다.
* `isna().sum()`으로 열별 결측값 개수를 확인할 수 있습니다.
* `ModuleNotFoundError`, `FileNotFoundError`, `KeyError`, 자료형 문제를 순서대로 점검할 수 있습니다.
* AI에게 새로운 분석 코드를 바로 요구하기보다 구조·결과 해석·다음 질문을 검토받을 수 있습니다.

---

## 1. Chapter 21 핵심 개념 정리

### 1) Series vs DataFrame 차이 및 열 선택
pandas에서 데이터를 선택할 때 사용하는 대괄호(`[]`)의 개수에 따라 반환되는 데이터의 차원과 자료구조가 달라집니다.

| 구분 | `a = df["score"]` | `b = df[["score"]]` |
| :--- | :--- | :--- |
| **대괄호 개수** | 1개 `[]` | 2개 `[[]]` |
| **자료구조 (Type)** | **Series** (`pandas.Series`) | **DataFrame** (`pandas.DataFrame`) |
| **차원** | **1차원** (단일 열 형태) | **2차원** (행과 열을 갖춘 표 형태) |
| **특징** | 열 이름이 부가 정보(`Name: score`)로 출력됨 | 표 형태로 출력되며, 여러 열(`["a", "b"]`) 선택 시에도 사용됨 |

---

## 2. 주요 Q&A 및 핵심 포인트

### Q1. `df["score"]`와 `df[["score"]]`의 차이는 무엇인가요?
* **`df["score"]` (Series):** 하나의 열을 1차원 리스트/배열처럼 가져옵니다. 상단에 열 헤더가 뜨지 않고 하단에 `Name: score, dtype: int64` 형태의 정보가 표시됩니다.
* **`df[["score"]]` (DataFrame):** 열이 하나뿐이더라도 2차원 **표(Table)** 형태로 가져옵니다. 안쪽 대괄호 `["score"]`가 리스트 역할을 하므로 여러 열을 동시에 추출할 때 동일한 방식을 사용합니다.