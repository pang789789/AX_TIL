# 📘 [학습 일지] AI와 함께하는 타이타닉 데이터 분석 (STEP 00 ~ STEP 08)

> **일시:** 2026년 9월 11일  
> **주제:** 타이타닉 데이터셋을 활용한 EDA 및 전처리 기초 파이프라인 완주 (Part 1)  
> **진도:** STEP 00 (전체 지도) ~ STEP 08 (시각화와 기본 패턴 확인)  
> **핵심 원칙:** 코드는 AI의 도움을 받되, 문제 정의·결과 해석·다음 작업 결정은 분석가가 직접 수행한다.  

---

## 1. 수업 개요 및 학습 원칙

* **AI와 나의 역할 분담:** AI는 코드 작성과 분석 후보 제안을 돕고, 나는 분석 목적 설정, 결과 검증, 다음 행동 판단을 담당한다.
* **원본 보존의 원칙:** 불러온 원본 데이터(df)는 절대 직접 수정하지 않고, 탐색 및 전처리 단계별 작업용 복사본(df_work, df_encoded 등)을 분리하여 사용한다.

---

## 2. STEP별 학습 노트 및 실습 기록

### STEP 00. 전체 분석 지도 보기
* **핵심 내용:** 전체 분석 흐름(데이터 로딩 ➔ 전처리 ➔ 시각화/EDA ➔ Feature 설계 ➔ 모델링 ➔ 서비스 배포)을 이해하고, 코드를 치기 전 분석 방향을 설정한다.
* **성공 기준:** AI가 만든 코드를 무작정 실행하지 않고 실행 결과를 직접 검증할 수 있는 상태를 만드는 것.

### STEP 01. 실행 환경 확인
* **핵심 내용:** 터미널 Python, VS Code Interpreter, Notebook Kernel이 동일한 환경을 바라보는지 확인한다.
* **실행 코드:**
  import sys
  from pathlib import Path
  import numpy as np
  import pandas as pd
  print("Python executable:", sys.executable)
  print("pandas:", pd.__version__)
  print("numpy:", np.__version__)

### STEP 02. 데이터 확보와 로딩
* **핵심 내용:** 프로젝트 루트 경로를 자동으로 탐색하여 `train.csv` (891행 x 12열) 데이터를 로드한다.
* **주의점:** 파일을 제대로 읽었다고 해서 데이터가 깨끗한 것은 아니며, 원본 기준 데이터인 `df`는 변수에 그대로 보존한다.
* **실행 코드:**
  df = pd.read_csv(data_path)
  print("shape:", df.shape)
  df.head()

### STEP 03. 데이터 구조와 품질의 첫인상 확인
* **핵심 내용:** `df.info()`, `df.describe()`, `df.isna().sum()`을 활용해 숫자형/문자열 컬럼 및 결측치 현황을 파악한다.
* **관찰 결과:** Age(177개), Cabin(687개), Embarked(2개)의 결측치를 확인했으며, PassengerId의 고유값 및 이상치를 체크함.

### STEP 04. 분석 문제와 Target 정의
* **핵심 내용:** 예측하고 싶은 정답(Target)인 `Survived` 컬럼의 분포를 확인한다.
* **문제 유형:** 생존(1)과 사망(0)으로 나뉘는 **이진 분류(Binary Classification)** 문제로 정의.
* **데이터 누수 주의:** 예측 시점에 미리 알 수 없는 정보나 Target 정보가 Feature에 포함되지 않도록 유의한다.

### STEP 05. 결측치 처리 (탐색용 전처리)
* **핵심 내용:** 원본을 보존하기 위해 `df_work = df.copy()` 작업본을 생성하고 결측치 처리 방안을 모색한다.
* **탐색용 기본 처리:**
  - Age: 중앙값(median)으로 대체
  - Embarked: 최빈값(mode)으로 대체
  - Cabin: 결측이 많아 즉시 삭제하지 않고 Deck 파생 Feature 후보로 보존
* **중요 구분:** EDA 단계의 대표값 대체는 '탐색용'이며, 실제 최종 모델링 단계에서는 Train/Test 분리 후 Train 기준으로 재학습해야 함.

### STEP 06. 불필요한 컬럼 검토
* **핵심 내용:** 각 컬럼의 역할에 따라 정책을 결정한다 (KEEP / EXCLUDE / DERIVE / DEFER).
* **주요 판단:**
  - PassengerId: 단순 식별자이므로 EXCLUDE
  - Name, Ticket, Cabin: 파생 변수를 만들 수 있는 재료이므로 삭제하지 않고 DERIVE 지정

### STEP 07. 범주형 데이터 변환 원리 이해
* **핵심 내용:** 머신러닝 모델이 이해할 수 있도록 범주형(문자열) 데이터를 수치화하는 원리를 익힌다 (학습용 Branch: `df_encoded`).
* **인코딩 적용 방식:**
  - Sex (이진 범주): 단순 Mapping 적용 (`male`: 0, `female`: 1)
  - Embarked (명목형 범주): 순서 관계가 없으므로 One-Hot Encoding (`pd.get_dummies()`) 원리 확인

### STEP 08. 시각화와 기본 패턴 확인
* **핵심 내용:** 무작정 그래프를 그리지 않고 "질문 ➔ 적절한 시각화 ➔ 관찰 및 해석" 순서로 진행한다.
* **주요 분석 질문 및 결과:**
  1. 전체 생존/비생존 비율 ➔ `sns.countplot(data=df_work, x="Survived")`
  2. 성별에 따른 생존율 차이 ➔ `sns.barplot` (여성 집단에서 높은 생존율 관찰)
  3. 객실 등급(Pclass)별 생존율 ➔ 1등석 승객의 생존율이 가장 높음
  4. 나이(Age) 분포와 생존 관계 ➔ `sns.histplot` (단, 중앙값 대체가 반영된 df_work임을 감안하여 해석)
  5. 요금(Fare)과 생존 관계 ➔ `sns.boxplot` (이상치 영향 고려)

---

## 3. 학습 요약 및 데이터 객체 역할 정리

* `df`: 원본 기준 데이터 (절대 수정하지 않음)
* `df_work`: STEP 05~08 탐색 및 EDA용 작업본 (탐색용 전처리 포함)
* `df_encoded`: STEP 07 인코딩 원리 학습용 임시 branch
* **소회:** 데이터 시각화나 통계 분석 시, 내가 적용한 전처리(예: Age 중앙값 대체)가 결과 그래프와 분포 모양에 영향을 준다는 점을 항상 염두에 두고 관찰과 해석을 분리해야 함을 배웠다.