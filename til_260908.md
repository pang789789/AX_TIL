# 📘 종합 학습 정리 노트 (PostgreSQL & pandas & DB 특강)

---

## 1. Python & 프로젝트 환경 설정

### 1.1 프로젝트 루트 자동 탐색 및 모듈화
* **문제 배경:** Jupyter Notebook이 하위 폴더(`notebooks/ch04`)에서 실행되면 `Path.cwd()`만 사용할 시 `FileNotFoundError`가 발생함.
* **해결 원리:** 현재 위치에서 부모 폴더로 올라가며 `data` 폴더의 존재 유무를 검사하는 함수 작성.
* **VS Code 모듈 색상 상실 원인:** `from course_utils.paths import ...` 구문에서 모듈 파일명(`paths.py`)이 실제 폴더 파일명과 다르거나, 프로젝트 최상단 루트 폴더에 위치하지 않으면 구문 하이라이팅이 풀리거나 에러가 발생함.

---

## 2. PostgreSQL & SQL 기초 (Chapter 03)

### 2.1 SQL 기본 CRUD 및 연산자
* **테이블 제어:** `CREATE TABLE`로 구조 정의, `INSERT INTO`로 데이터 삽입.
* **조회 및 조건:** `WHERE` 절에서 `AND`, `OR`, `IN`, `LIKE` 사용. `NULL` 조건 검사는 `=` 대신 `IS NULL` / `IS NOT NULL`로 수행.
* **데이터 수정/삭제 안전 원칙:** `UPDATE` 및 `DELETE` 수행 전, **동일 조건의 `SELECT` 문을 먼저 실행해 대상 행을 검증**함.

---

## 3. pandas 데이터 분석 (Chapter 04)

### 3.1 구조 및 집계 지표 구분
* **Series vs DataFrame:**
  * **Series (1차원):** `df["column_name"]`
  * **DataFrame (2차원):** `df[["col1", "col2"]]`
* **집계 메서드 차이:**
  * `count()`: 결측치(`NaN`)를 제외한 데이터 개수.
  * `size()`: 그룹 내 전체 행 수 (결측치 포함).
  * `nunique()`: 고유한 값의 개수.
* **경로 연산 주의:** `pathlib.Path` 객체와 문자열 결합 시 `+` 연산자는 `TypeError`를 발생시키므로 `/` 연산자를 사용 (`path / "file.csv"`).

---

## 4. 데이터베이스 설계 및 실무 특강

### 4.1 핵심 실무 프로세스
* **학습 순서:** 요구사항 분석 $\rightarrow$ 테이블/속성 설계 $\rightarrow$ PK/FK 정의 $\rightarrow$ ERD 작성 $\rightarrow$ 마스터 데이터 생성 $\rightarrow$ SQL 작성/검증 $\rightarrow$ 대시보드 집계.
* **PostgreSQL 공식 문서 활용:** 기능을 외우기보다 공식 문서 문법 예제를 내 테이블과 열 이름에 맞춰 변형하여 사용.

### 4.2 카페 주문 시스템 설계예시 (`orders` vs `order_items`)

| 구분 | 주문 (`orders`) 테이블 | 주문 상세 (`order_items`) 테이블 |
| :--- | :--- | :--- |
| **비유** | **영수증 전체 (결제 1건)** | **영수증 속 개별 상품 줄** |
| **한 행의 의미** | 고객이 한 번 결제한 거래 1건 | 한 주문 내 포함된 개별 메뉴 1개 |
| **핵심 지표** | • **오늘 총 주문 건수** (`COUNT(*)`)<br>• 오늘 총 매출액 (`SUM(total_amount)`)<br>• 주문 상태별 건수 | • **많이 판매된 메뉴** (`SUM(quantity)`)<br>• 메뉴 종류별 판매 금액<br>• 판매된 총 상품 항목 수 |

* **"오늘 주문 건수" 집계 위치:** 주문 건수는 결제 1건(영수증 1장)을 의미하므로 **`orders` 테이블 기준**으로 집계함. (`order_items` 행 수를 세면 손님이 여러 상품을 같이 주문했을 때 주문 건수가 과다 집계됨)