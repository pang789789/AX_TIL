[파이썬 기초] AI 활용 파이썬 입문부터 디버깅까지
1. 학습 목표
이 포스팅을 마치면 다음의 핵심 파이썬 역량을 가질 수 있습니다.

기초 실행 흐름 파악: 파이썬 코드를 읽고 기본 실행 순서를 설명할 수 있습니다.

핵심 문법 활용: 변수, 조건문, 반복문, 리스트, 딕셔너리, 함수 등을 직접 작성하고 활용합니다.

데이터 및 파일 처리: 파일과 CSV 데이터를 읽어 간단한 프로그램을 구현하고, pandas로 데이터를 둘러볼 수 있습니다.

디버깅 및 AI 협업: 오류 메시지에서 단서를 찾아 해결하며, 생성형 AI에게 완성 코드를 맡기기보다 힌트·코드 리뷰·디버깅을 요청하고 검증합니다.

문제 해결 프로세스: 작은 문제를 요구사항 → 설계 → 구현 → 실행 → 검증의 흐름으로 다룹니다.

2. 핵심 영어 용어 정리
VS Studio (Visual Studio Code): 코드를 작성하고 실행할 수 있는 대표적인 개발용 텍스트 에디터입니다.

PS (PowerShell): 윈도우 환경에서 명령어(텍스트)를 입력해 컴퓨터 및 프로그램을 제어하는 터미널입니다.

CSV (Comma-Separated Values): 데이터를 쉼표(,)로 구분하여 저장한 파일 형식입니다.

pandas: 파이썬에서 표(테이블) 구조의 데이터를 쉽게 읽고 분석할 수 있게 돕는 데이터 분석 라이브러리입니다.

df (DataFrame): pandas에서 행과 열로 이루어진 표 형태의 데이터 객체를 의미합니다.

print(): 괄호 안의 내용을 터미널 화면에 출력하는 파이썬 기본 함수입니다.

if / else: 조건에 따라 코드 실행 흐름을 제어하는 조건문입니다.

for: 특정 데이터 항목을 순서대로 반복할 때 사용하는 반복문입니다.

def (Define): 재사용 가능한 코드 묶음인 함수를 정의하는 키워드입니다.

return: 함수 내부 실행 결과를 외부로 반환하는 키워드입니다.

head(): DataFrame 데이터의 상단 일부(기본 5개)를 출력하여 구조를 확인하는 함수입니다.

3. 프로그램의 기본 진행 순서 및 예시
모든 프로그램은 기본적으로 입력 → 처리 → 출력 구조로 작동합니다.

Plaintext
[ 입력 (Input) ]  ───>  [ 처리 (Process) ]  ───>  [ 출력 (Output) ]
[실생활 예시 문제]

Q. 온라인 쇼핑몰에서 주문 금액을 계산한다.

A.

입력: 상품 가격, 수량

처리: 가격 * 수량

출력: 총 주문 금액

4. 파이썬 기초 및 단계별 예제 (STEP 1 ~ STEP 7)
4.1. 처음 배운 조건문 코드 및 VS Code 터미널 실행
작성 코드 (00_01.py)

Python
score = 85
if score >= 80:
    print("통과")
VS Code PowerShell 실행 기록

Bash
PS C:\dev\python_temp> python 00_01.py
통과
PS C:\dev\python_temp> python 00_01.py
통과
PS C:\dev\python_temp> python 00_01.py
PS C:\dev\python_temp> python 00_01.py
불합격
4.2. 단계별 파이썬 구현 (STEP 1 ~ STEP 7)
STEP 1. 화면에 메시지 출력하기

Python
print("오늘의 주문을 확인합니다.")
STEP 2. 변수에 데이터 저장하기

Python
customer = "민수"
amount = 45000
print(customer, amount)
STEP 3. 조건에 따라 판단하기

Python
if amount >= 50000:
    print("무료배송")
else:
    print("배송비 3,000원")
STEP 4. 여러 주문 처리하기 (for 반복문)

Python
orders = [45000, 72000, 38000]
for amount in orders:
    print(amount)
STEP 5. 데이터 구조화하기 (리스트 + 딕셔너리)

Python
orders = [
    {"customer": "민수", "amount": 45000},
    {"customer": "지영", "amount": 72000}
]
STEP 6. 함수로 기능 분리하기

Python
def calculate_shipping(amount):
    if amount >= 50000:
        return 0
    return 3000
STEP 7. CSV 데이터 분석하기 (pandas)

Python
import pandas as pd

df = pd.read_csv("orders.csv")
print(df.head())
5. 파이썬 for문 실생활 예시
for문은 리스트나 범위(range) 같은 데이터 묶음에서 항목을 하나씩 꺼내 반복 실행합니다.

출석부 이름 부르기

Python
students = ["민수", "지영", "현우"]

for student in students:
    print(f"{student} 학생, 출석 확인되었습니다.")
카페 주문 금액 계산하기

Python
orders = [4500, 5000, 3500]
total = 0

for price in orders:
    total += price

print(f"총 결제 금액은 {total:,}원입니다.")
대기 번호표 출력 (range 활용)

Python
for num in range(1, 6):
    print(f"{num}번 대기자님, 창구로 오세요.")
6. AI를 활용한 코드 작성 및 디버깅 실습
6.1. 기본 코드 구현
상품 가격 12,000원, 수량 3개일 때의 계산 코드입니다.

Python
# 입력
price = 12000
quantity = 3

# 처리
total_price = price * quantity

# 출력
print(f"총 주문 금액: {total_price:,}원")
6.2. 의도적 변수 오류와 디버깅 (NameError)
아래 코드는 출력 시 선언되지 않은 변수 total을 호출하도록 변경한 코드입니다.

Python
# 입력
price = 12000
quantity = 3

# 처리
total_price = price * quantity

# 출력 (오류 발생 지점: total_price 대신 total 사용)
print(f"총 주문 금액: {total:,}원")
터미널 오류 로그

Bash
PS C:\dev\python_temp> python 00_02.py
Traceback (most recent call last):
  File "C:\dev\python_temp\00_02.py", line 9, in <module>
    print(f"총 주문 금액: {total:,}원")
                            ^^^^^
NameError: name 'total' is not defined
원인 및 해결

원인: 변수를 정의할 때는 total_price로 선언했으나 출력 시 total이라는 없는 변수를 참조함.

해결: total을 total_price로 수정.

7. AI 활용 및 질문 팁
코드 작성 중 오류가 발생할 때는 단순히 스크린샷만 남기지 말고 텍스트 형태의 정확한 정보를 전달해야 AI로부터 명확한 답을 얻을 수 있습니다.

좋은 질문을 구성하는 4가지 핵심 요소

환경 정보: 사용 중인 OS 및 에디터 (예: Windows 11, VS Code)

실행한 명령: 터미널에서 실행한 실제 명령어 (예: python 00_02.py)

오류 메시지: 터미널의 에러 로그 전체 텍스트

원하는 도움: 구체적으로 요구할 사항 (예: 원인 설명, 수정 방법, 디버깅 순서 등)