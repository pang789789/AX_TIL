# Git 핵심 개념 정리: Branch 작업과 Pull Request (PR)a

프로젝트 개발을 진행하면서 접하게 되는 Git 버전 관리의 핵심 흐름과 용어를 정리합니다.

---

## 1. Branch 생성 후 작업이란?

"Branch(브랜치) 생성 후 작업"이란 기존 메인 코드(주로 `main` 또는 `master`)와 분리된 **독립된 작업 공간을 만든 뒤, 그곳에서 안전하게 코드를 수정하고 기능을 개발하는 방식**을 의미합니다.

### 💡 쉬운 비유로 이해하기

* **메인 코드 (`main`):** 손님에게 실제 제공 중인 **완성된 본품(배포용)**
* **새 브랜치 (Feature Branch):** 본품을 그대로 복사해 온 **연습용 사본**

메인 코드를 직접 수정하다가 오류가 발생하면 서비스 전체가 멈출 수 있습니다. 따라서 안전한 복사본(새 브랜치)을 만들어 충분히 개발 및 테스트를 거친 뒤 메인 코드에 합치는 방식을 사용합니다.

---

## 2. Issue 기반 Branch 전략

실무 개발 환경에서는 다음과 같은 규칙을 바탕으로 작업합니다.

> 1. **`main` 브랜치에서는 여러 사람이 직접 동시에 작업하지 않습니다.**
> 2. **하나의 Issue(할 일)마다 별도의 Branch를 생성하여 작업합니다.**
> 
> 

### 📌 왜 이렇게 작업하나요?

* **안전성:** 작업 중 에러가 발생해도 운영 중인 `main` 코드에는 전혀 영향을 주지 않습니다.
* **동시성:** 여러 개발자가 서로의 코드를 건드리지 않고 각자 맡은 기능을 동시에 개발할 수 있습니다.
* **이슈 추적:** `fix/issue-42-login`처럼 작업 단위(이슈)별로 브랜치를 관리하면 변경 이력을 명확하게 추적할 수 있습니다.

---

## 3. Pull Request (PR)란?

**PR은 'Pull Request'의 약자로, 작업한 브랜치의 코드를 메인 브랜치에 합쳐달라고 요청하는 절차**입니다.

팀원들이 각자 작성한 코드를 검증 없이 `main`에 올리면 서비스가 동작하지 않는 문제가 발생할 수 있습니다. 이를 방지하기 위해 "내가 작성한 코드를 검토해 보고 이상이 없으면 메인 프로젝트에 통합해 주세요"라고 요청하는 단계입니다.

### 🛠 협업 및 프로젝트 관리에서의 PR 역할

* **코드 리뷰 (Code Review):** 팀원 또는 프로젝트 관리자가 변경 사항을 미리 확인하여 버그나 오타를 방지합니다.
* **최종 통합 (Merge):** 리뷰 및 테스트가 완료된 코드만 메인 브랜치에 안전하게 병합합니다.

---

## 🔄 전체 작업 워크플로우 (Workflow)

```text
[Issue 생성/할당] ➔ [main에서 전용 Branch 생성] ➔ [기능 개발 및 Commit] ➔ [PR 작성] ➔ [코드 리뷰 및 Approve] ➔ [main에 Merge]

```

1. **이슈 확인:** 해결해야 할 과제나 버그(Issue)를 확인합니다.
2. **브랜치 생성:** `main` 브랜치를 기준으로 작업용 브랜치를 생성합니다.
3. **코드 작업:** 해당 브랜치에서 기능 개발을 진행하고 저장(Commit)합니다.
4. **PR 작성:** GitHub 등에서 메인 브랜치로 합쳐달라는 Pull Request를 생성합니다.
5. **검토 및 통합:** 코드 리뷰를 거쳐 승인(Approve)되면 `main` 브랜치로 최종 통합(Merge)합니다.