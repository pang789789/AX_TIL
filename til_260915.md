# 📘 [학습 일지] Chapter 06. 정규화/데이터 무결성 & 질문으로 시작하는 EDA

> **일시:** 2026년 9월 15일  
> **주제:** DB 정규화(1NF~3NF) 및 무결성 제약조건 / 데이터를 보며 질문을 만드는 EDA 파이프라인  
> **학습 방식:** 1:1 질의응답 중심의 개념 검증, SQL 실습 및 탐색적 데이터 분석(EDA) 원칙 정리  

---

## 1. Part 1. 데이터베이스 정규화 (Normalization) & 무결성

### 💡 정규화의 본질
* 정규화는 단순히 테이블을 쪼개는 것이 아니라, 하나의 큰 테이블에 섞여 있는 데이터들의 **"주인(소유자)"을 찾아 테이블을 의미 있게 나누는 과정**이다.
* **이상 현상 (Anomaly) 3가지:**
  1. **삽입 이상:** 대여 이력이 없는 신규 도서를 등록하려 할 때, 회원/대여 정보가 없어 추가하기 어려움.
  2. **수정 이상:** 회원의 이메일이 변경되었을 때, 수십 개의 대여 이력을 일일이 수정해야 하며 누락 시 불일치 발생.
  3. **삭제 이상:** 특정 대여 기록을 지웠더니 해당 도서의 유일한 정보까지 함께 삭제됨.

### 💡 주요 정규형 (1NF ~ 3NF) 핵심 원칙
* **1NF (제1정규형):** 한 셀에 여러 값이 쉼표나 배열로 묶여있지 않고 원자값(1행 1값)을 갖도록 분리.
* **2NF (제2정규형):** 복합키 일부에만 의존하는 열(부분 종속)을 별도 테이블로 분리.
* **3NF (제3정규형):** 기본키가 아닌 일반 열들끼리 서로를 결정하는 전이 종속 구조를 분리.

### 💡 Q&A: 무결성 검사와 SQL 쿼리 장단점 비교
* **Q. 등록되지 않은 도서의 대출 내역 조회 시 하드코딩 vs 중첩 쿼리(서브쿼리)의 차이는?**
  - **하드코딩 (`WHERE book_id NOT IN (201, 202)`):** 속도는 빠르나 수동 수정이 필요하여 데이터 동기화 및 실시간성이 떨어짐.
  - **중첩 쿼리 (`WHERE book_id NOT IN (SELECT id FROM books_nf)`):** 실시간 DB 상태를 반영하여 자동화되나, `NULL` 값 포함 시 0건이 나오는 위험성이 있음.
  - **실무 권장:** `NULL` 처리 위험을 방지하고 인덱스를 타는 **`LEFT JOIN ... WHERE b.id IS NULL`** 또는 **`NOT EXISTS`** 방식 활용.

---

## 2. Part 2. Chapter 06. 데이터를 보며 질문을 만드는 EDA

### 💡 EDA의 본질과 흐름
* EDA(Exploratory Data Analysis)는 단순히 그래프를 많이 그리는 작업이 아니라, **"데이터로 확인 가능한 질문을 만들고 검증하는 과정"**이다.
* **핵심 흐름:**
  질문 만들기 ➔ 지표 정의 ➔ 계산 범위 정하기 ➔ 데이터 집계 ➔ 결과 검증 ➔ 관찰/해석 구분 ➔ 다음 질문 만들기

### 💡 핵심 정리 및 실습 원칙
1. **모호한 질문을 측정 가능한 지표로 전환:**
   - 예: "어떤 상품이 인기 있는가?" ➔ "카테고리별 completed 주문 수량(total_quantity)과 금액(total_sales)은 어떻게 다른가?"
2. **관찰, 가설, 추가 검증의 엄격한 분리:**
   - **관찰:** 데이터에서 직접 확인한 사실 (예: 3월 completed 주문 금액이 2월보다 높음)
   - **가설:** 관찰을 설명할 수 있는 가능성 (예: 주문 수 증가 또는 평균 주문 금액 증가 영향 가능성)
   - **한계 및 추가 검증:** 현재 데이터에 없는 원인(마케팅 프로모션 등)을 함부로 단정하지 않고 추가 분석 수행.
3. **단변량 ➔ 이변량 ➔ 다변량 EDA 확장:**
   - 단순한 변수 하나(고객 연령대, 도시 등)부터 시작하여 두 변수의 관계, 다변량 조합으로 구체화.
4. **금액성 EDA의 `completed` Gate 원칙:**
   - `order_items` 행 중 실제 결제 완료된 주문만 다루기 위해 `order_status == "completed"` 필터를 적용 후 분석.
   - 다대일 병합 전후 행 수, 미매칭 건수(0건)를 반드시 확인하여 중복 계산 방지.
5. **총합 검증 (Source Total Consistency):**
   - 카테고리별 합계, 월별 합계, 고객별 합계를 다시 다 더했을 때 원본 `source_total`과 일치하는지 계산 검증.

---

## 3. Today's Summary & Takeaway

* 데이터베이스 설계와 전처리/EDA 모두 **"데이터의 주인과 정당한 범위를 정의하는 일"**에서 출발한다.
* SQL과 Python 코드 실행 결과가 출력되었다고 해서 바로 해석으로 넘어가선 안 되며, **무결성 제약조건 및 병합 전후 행 수 검증**을 거쳐야 분석 결과를 신뢰할 수 있다.
* LLM은 질문 후보를 확장하고 최소 코드를 작성하는 도구로 활용하되, 데이터 범위 검증과 최종 해석 책임은 분석가 본인에게 있다.