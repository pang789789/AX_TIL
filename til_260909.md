# 📘 Chapter 04. pandas로 데이터에 질문하기 (종합 정리 노트)

---

## 1. 분석 범위 및 용어 정의 (비즈니스 기준 고정)

데이터 분석 시 모호한 수식어를 제거하고 명확한 기준을 정의해야 올바른 결과를 얻을 수 있습니다.

| 모호한 표현 | 명확한 분석 기준 | 적용 코드 및 설명 |
| :--- | :--- | :--- |
| **매출** | 완료(completed) 주문의 line_total 합계 | completed_sales["line_total"].sum() |
| **주문 수** | 고유한 주문 번호 개수 | df["order_id"].nunique() |
| **고객 수** | 고유한 고객 번호 개수 | df["customer_id"].nunique() |
| **판매량** | 구매 수량의 총합 | df["quantity"].sum() |

---

## 2. 파생 컬럼 생성 (line_total) 및 전체 금액 구분

### 2.1 line_total 파생 컬럼 계산
* **개념:** 주문 상세(개별 상품 줄) 1행의 금액 (구매 수량 x 구매 당시 단가)

# 원본 보호를 위한 작업용 복사본 생성
order_items_work = order_items.copy()

# 파생 컬럼 생성
order_items_work["line_total"] = (
    order_items_work["quantity"] * order_items_work["unit_price"]
)

# 첫 번째 행 수작업 대조 검증
sample = order_items_work.iloc[0]
print(sample["quantity"] * sample["unit_price"] == sample["line_total"])  # True

### 2.2 전체 주문상세 금액 vs 완료 주문 기준 매출
* **전체 주문상세 금액 (all_order_amount):** `order_items_work["line_total"].sum()`
  * *주의:* 취소(cancelled), 환불(refunded)된 주문이 포함되어 있으므로 '매출'이 아닌 '전체 주문상세 금액'으로 표현.
* **완료 주문 기준 매출 (completed_revenue):** `orders` 테이블과 병합(merge) 후 `order_status == 'completed'` 조건으로 필터링하여 계산.

---

## 3. 데이터 구조 선택 및 조건 필터링

### 3.1 Series vs DataFrame 차이
* **Series (1차원):** `customers["city"]` (조건식 생성, 통계 계산용)
* **DataFrame (2차원):** `customers[["customer_id", "city"]]` (복수 열 선택, 병합용)

### 3.2 조건 필터링 연산자

# AND(&): 30세 이상이면서 서울 거주
seoul_over_30 = customers[(customers["age"] >= 30) & (customers["city"] == "서울")]

# OR(|) 및 isin(): 서울 또는 부산 거주
seoul_or_busan = customers[customers["city"].isin(["서울", "부산"])]

# NOT(~): 완료 상태가 아닌 주문
not_completed = orders[~(orders["order_status"] == "completed")]

---

## 4. 데이터 집계(Aggregation) 및 unique 구분

### 4.1 집계 메서드 역할
* `count()`: 결측치(NaN)를 제외한 데이터 개수
* `size()`: 결측치를 포함한 그룹 내 전체 행 수
* `nunique()`: 고유한 값의 개수 (실제 주문 건수, 고객 수 계산 시 필수)

### 4.2 unique() vs nunique()
* `unique()`: 고유한 값의 목록(배열) 반환 -> ['서울', '부산']
* `nunique()`: 고유한 값의 개수(숫자) 반환 -> 2

### 4.3 groupby() + agg() 사용 예시

# 입력 데이터: products (상품 5개)
category_summary = (
    products
    .groupby("category", as_index=False)
    .agg(
        product_count=("product_id", "nunique"),  # 고유 상품 수
        average_price=("price", "mean"),          # 평균 가격
        maximum_price=("price", "max")            # 최고 가격
    )
)

**[실행 결과 예시]**

| category | product_count | average_price | maximum_price |
| :--- | :--- | :--- | :--- |
| **디저트** | 2 | 4750.0 | 6500 |
| **음료** | 3 | 4833.3 | 5500 |

---

## 5. 날짜 데이터 처리 (.dt Accessor)

데이터 타입이 datetime64일 때 날짜/시간 정보를 추출하는 속성입니다.

# 날짜 타입 변환
orders["order_date"] = pd.to_datetime(orders["order_date"])

# 파생 컬럼 추출
orders["order_year"] = orders["order_date"].dt.year
orders["order_month"] = orders["order_date"].dt.month
orders["order_year_month"] = orders["order_date"].dt.strftime("%Y-%m")

---

## 6. 안전한 데이터 병합 (merge) 및 저장

### 6.1 병합 검증 옵션

order_sales = order_items_work.merge(
    orders[["order_id", "customer_id", "order_date", "order_status"]],
    on="order_id",
    how="left",
    validate="many_to_one",  # 관계 검증 (오른쪽 키 중복 시 에러 발생)
    indicator="order_match"  # 매칭 출처 확인 ('both', 'left_only')
)

### 6.2 CSV 저장 규칙

# 인덱스 제외, 한글 깨짐 방지 옵션(utf-8-sig) 적용
category_summary.to_csv(
    "reports/category_sales.csv", 
    index=False, 
    encoding="utf-8-sig"
)